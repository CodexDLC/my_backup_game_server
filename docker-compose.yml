# docker-compose.yml

# ===================================================================
# volumes & networks: Определяем их на верхнем уровне
# ===================================================================
volumes:
  pg_data:
    driver: local
  redis_bot_local_data:
    driver: local
  redis_central_data:
    driver: local
  mongo_data: # ✅ НОВОЕ: Том для персистентных данных MongoDB
    driver: local

networks:
  backend_network:
    driver: bridge

# ===================================================================
# x-templates: Используем якоря для переиспользования общих блоков
# ===================================================================
x-common-env: &common-env
  env_file: .env

x-common-restart-policy: &common-restart-policy
  restart: always

x-common-build-python: &common-build-python
  build:
    context: .
    # Dockerfile будет указан в каждом сервисе отдельно

# ===================================================================
# 🏗️ ИНФРАСТРУКТУРНЫЕ СЕРВИСЫ
# ===================================================================
services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres_service
    hostname: postgres
    <<: [*common-env, *common-restart-policy]
    networks: [backend_network]
    environment:
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    volumes:
      - pg_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  mongo_db: # ✅ НОВОЕ: Сервис MongoDB
    image: mongo:6.0 # Рекомендуется указывать конкретную версию
    container_name: mongo_service # Имя контейнера для MongoDB
    hostname: mongo # Имя хоста для внутренней сети Docker
    <<: [*common-env, *common-restart-policy] # Используем общие настройки окружения и перезапуска
    networks: [backend_network]
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME} # Читаем из .env
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD} # Читаем из .env
      MONGO_INITDB_DATABASE: ${MONGO_DB_NAME} # Опционально: создаст базу данных при первом запуске
    ports:
      - "27017:27017" # Пробрасываем стандартный порт MongoDB на хост
    volumes:
      - mongo_data:/data/db # Сохранение данных в именованный том
    healthcheck: # Проверка состояния MongoDB
      test: echo 'db.runCommand("ping").ok' | mongosh --quiet # Простая команда для проверки работоспособности
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s # Добавлено для консистентности с healthcheck postgres

  redis: # Центральный REDIS (для Backend)
    image: redis:7.2-alpine
    container_name: redis_cache_service
    hostname: redis
    <<: [*common-env, *common-restart-policy]
    networks: [backend_network]
    command: redis-server --requirepass "${REDIS_PASSWORD}" --appendonly yes
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "$${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - redis_central_data:/data

  redis_bot_local: # ЛОКАЛЬНЫЙ REDIS ДЛЯ DISCORD-БОТА
    image: redis:7.2-alpine
    container_name: redis_bot_local_cache
    hostname: redis_bot_local
    <<: [*common-env, *common-restart-policy]
    networks: [backend_network]
    command: redis-server --appendonly yes
    volumes:
      - redis_bot_local_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

# ===================================================================
# 🚀 ПРИЛОЖЕНИЯ И КООРДИНАТОРЫ
# ===================================================================
  fast_api:
    <<: [*common-env, *common-restart-policy, *common-build-python]
    networks: [backend_network]
    build:
      dockerfile: services_config/rest_api/Dockerfile
    container_name: fast_api_service
    command: gunicorn -k uvicorn.workers.UvicornWorker -w 1 -b 0.0.0.0:8000 game_server.api_fast.main:app
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://redis:6379/0
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      # ✅ НОВЫЕ: Передача переменных MongoDB в контейнер fast_api
      - MONGO_URI=${MONGO_URI}
      - MONGO_HOST=${MONGO_HOST_CONTAINER}
      - MONGO_PORT=${MONGO_PORT}
      - MONGO_DB_NAME=${MONGO_DB_NAME}
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_INITDB_ROOT_USERNAME}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_INITDB_ROOT_PASSWORD}
      - CONTAINER_ID=fast_api_service # <--- ДОБАВЛЕНО: Передача имени контейнера как ID
    depends_on:
      postgres: { condition: service_healthy }
      redis: { condition: service_healthy }
      mongo_db: { condition: service_healthy } # ✅ НОВАЯ ЗАВИСИМОСТЬ
    volumes:
      - ./logs/fast_api_output:/app/game_server/logs
    healthcheck: # ДОБАВЬТЕ ЭТОТ БЛОК, ЕСЛИ ЕГО НЕТ
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"] # Убедитесь, что эндпоинт /health есть
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s # Дать время на запуск FastAPI

  discord_bot:
    <<: [*common-env, *common-restart-policy, *common-build-python]
    networks: [backend_network]
    build:
      dockerfile: services_config/Discord_API/Dockerfile
    container_name: discord_bot_service
    command: python -m Discord_API.main
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - REDIS_LOCAL_URL=redis://redis_bot_local:6379/0
      - GAME_SERVER_API=http://fast_api:8000 # Используем имя сервиса fast_api
      - CONTAINER_ID=discord_bot_service # <--- ДОБАВЛЕНО: Передача имени контейнера как ID
    depends_on:
      fast_api: { condition: service_started }
      redis_bot_local: { condition: service_healthy }
    volumes:
      - ./logs/discord_bot_output:/app/game_server/logs

  start_orchestrator:
    <<: [*common-env, *common-restart-policy, *common-build-python]
    networks: [backend_network]
    build:
      dockerfile: services_config/game_server/Dockerfile
    container_name: start_orchestrator_service
    command: uvicorn game_server.game_services.game_world_state_orchestrator:app --host 0.0.0.0 --port 8000
    
    # 🔥 ДОБАВЛЕНО: Переменные окружения для подключения к зависимостям
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=redis://redis:6379/0
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      # ✅ НОВЫЕ: Передача переменных MongoDB
      - MONGO_URI=${MONGO_URI}
      - MONGO_HOST=${MONGO_HOST_CONTAINER}
      - MONGO_PORT=${MONGO_PORT}
      - MONGO_DB_NAME=${MONGO_DB_NAME}
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_INITDB_ROOT_USERNAME}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_INITDB_ROOT_PASSWORD}
      - CONTAINER_ID=start_orchestrator_service # <--- ДОБАВЛЕНО: Передача имени контейнера как ID
    
    # 🔥 ДОБАВЛЕНО: Зависимость от Redis и MongoDB
    depends_on:
      postgres: { condition: service_healthy }
      redis: { condition: service_healthy }
      mongo_db: { condition: service_healthy } # ✅ НОВАЯ ЗАВИСИМОСТЬ
      
    volumes:
      - ./logs/start_orchestrator_output:/app/game_server/logs # Обновлен путь для консистентности


# ===================================================================
# 🔥 НОВЫЙ СЕРВИС: ARQ ВОРКЕР 🔥
# ===================================================================

  arq_worker:
    <<: [*common-env, *common-restart-policy, *common-build-python]
    networks: [backend_network]
    build:
      dockerfile: services_config/game_server/Dockerfile
    container_name: arq_worker_service
    
    # ИСПРАВЛЕНИЕ: УДАЛЕНА ОПЦИЯ --watch
    command: arq game_server.Logic.InfrastructureLogic.arq_worker.arq_worker_settings.WorkerSettings
    
    # Воркеру нужны те же переменные окружения для подключения к БД и Redis
    environment:
      - REDIS_URL=redis://redis:6379/0
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      # ✅ НОВЫЕ: Передача переменных MongoDB
      - MONGO_URI=${MONGO_URI}
      - MONGO_HOST=${MONGO_HOST_CONTAINER}
      - MONGO_PORT=${MONGO_PORT}
      - MONGO_DB_NAME=${MONGO_DB_NAME}
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_INITDB_ROOT_USERNAME}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_INITDB_ROOT_PASSWORD}
      - CONTAINER_ID=arq_worker_service # <--- ДОБАВЛЕНО: Передача имени контейнера как ID
      
    # Воркер должен стартовать только после того, как БД, Redis и MongoDB готовы
    depends_on:
      postgres: { condition: service_healthy }
      redis: { condition: service_healthy }
      mongo_db: { condition: service_healthy } # ✅ НОВАЯ ЗАВИСИМОСТЬ      
    volumes:
      - ./logs/arq_worker_output:/app/game_server/logs
